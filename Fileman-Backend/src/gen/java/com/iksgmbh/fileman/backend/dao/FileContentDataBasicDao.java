package com.iksgmbh.fileman.backend.dao;

import java.util.List;

import org.springframework.stereotype.Component;

import com.iksgmbh.fileman.backend.FileContentData;

import javax.persistence.*;
import javax.persistence.criteria.*;

import org.springframework.stereotype.Repository;
import org.springframework.transaction.annotation.Transactional;

@Component
/**
 * Implementation of basic dao functionalities.
 * This file is autogenerated by MOGLiCC. Do not modify manually!
 */
@Repository
@Transactional
public class FileContentDataBasicDao
{
	@PersistenceContext
	protected EntityManager entityManager;

	public List<FileContentData> findAllFileContentDatas() {
		CriteriaQuery<FileContentData> criteria = entityManager.getCriteriaBuilder().createQuery(FileContentData.class);
		criteria.select(criteria.from(FileContentData.class));
		return entityManager.createQuery(criteria).getResultList();
	}

	public FileContentData findByUuid(Long uuid) {
		return entityManager.find(FileContentData.class, uuid);
	}

	public boolean update(FileContentData entity) {
		try {
			entityManager.merge(entity);
			return true;
		} catch (Exception e) {
			return false;
		}
	}

	public List<FileContentData> findAllForName(String toSearch) {
        CriteriaBuilder criteriaBuilder = entityManager.getCriteriaBuilder();
        CriteriaQuery<FileContentData> criteria = criteriaBuilder.createQuery(FileContentData.class);
        Root<FileContentData> fileContentData = criteria.from(FileContentData.class);
        criteria.where(criteriaBuilder.equal(fileContentData.get("name"), toSearch));
        return entityManager.createQuery(criteria).getResultList();
	}


	public FileContentData create(FileContentData entity) {
		entityManager.persist(entity);
		return entity;
	}

	public void delete(FileContentData entity) {
		entityManager.remove(entity);
	}
}